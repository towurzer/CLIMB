// One definition of how a scene is identified on the client.
// had this distributed inline and nothing worked :/, so if it ever changes again, easy fix
export const sceneKey = (sceneId) => String(sceneId);
